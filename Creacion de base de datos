Agregre esto a su gestor de base de datos

el puerto de conexion en mi caso era el 3306

create database hola;
use hola;

CREATE TABLE log (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(44),
    apellido VARCHAR(50),
    Telefono VARCHAR(20),
    Correo VARCHAR(44),
    usuario VARCHAR(44),
    contraseña VARCHAR(44)
);

quitar el modo seguro de el gestor de base de datos por si acaso 


